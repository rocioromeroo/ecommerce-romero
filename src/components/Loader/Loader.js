import "./Loader.css"

const Loader = (() => {
    return (
        <div className="loader">
            <img src="/images/loader.gif" alt="loading"></img>
        </div>
    )
})

export default Loader;