import "./Loader.css"

const Loader = (() => {
    return (
        <div className="loader">
            <img src="/loading.gif" alt="loading"></img>
        </div>
    )
})

export default Loader;